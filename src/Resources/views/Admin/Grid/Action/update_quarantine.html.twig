{% if data.quarantineItem and data.quarantineItem.isQuarantined() %}
    {{ include('@MonsieurBizSyliusAntiSpamPlugin/Admin/Grid/Action/_update_quarantine_button.html.twig', { 'data': data.quarantineItem, 'options': options|default([]) }) }}
{% endif %}
